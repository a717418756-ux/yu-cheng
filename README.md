# Y.C. 多功能專用平台

> 警察特考備考 PWA｜版本 **4.6.0**
> 離線可用．資料存本機．支援電子紙

---

# 第一部分：操作說明

## 一、三大分區

| 分區 | 內容 |
|------|------|
| **考試區** | 題庫練習、間隔複習、申論練習、法規資料庫 |
| **休閒區** | 電子書（epub/PDF）、影音庫 |
| **成長區** | 英語學習庫、運動健康 |

---

## 二、測驗系統

### 四個入口

| 入口 | 出題範圍 |
|------|---------|
| **今日複習** | 選擇題中，遺忘曲線到期的題目 |
| **加強複習** | 危險題 ∪ 收藏題（去重） |
| **申論練習** | 申論題（type = es） |
| **數字魔鬼** | 數字記憶訓練 |

四個入口都可**多選科目**，選擇會被記住。不限題數，底部「結束」鈕可隨時看統計。

### 遺忘曲線（間隔重複）

```
答對 → 升級，間隔 1 → 3 → 7 → 14 → 30 → 60 → 180 天
答錯 → 降回一半（如 180天 → 14天），隔天重考
```

高等級答錯需**連續答對數次**才會回到長間隔，避免「僥倖答對一次就當熟練」。

### 危險題判定

「最近 3 次全錯」或「錯誤率 > 50% 且答過 2 次以上」。

> **注意**：題庫中未填正確答案的題目，作答時會提示且**不計入複習進度與統計**，不會污染錯題分析。

---

## 三、答題功能

### 底部按鈕（作答後才出現）

| 按鈕 | 功能 |
|------|------|
| ☆ | 收藏此題 |
| ✏️ | **手寫標註**（開啟後不能捲動，專心畫） |
| 📄 | **文字備註**（與題庫的「備註／詳解」同一欄位，雙向同步） |
| 下一題 → | 進入下一題 |
| 結束 | 隨時結束並看統計 |

### 手寫標註工具列

```
[紅][黃][綠][藍][黑][⊕自訂]  [細][中][粗]
[✎ 手繪] [╱ 直線] [▭ 螢光]
[↶ 復原] [清除] [✓ 完成]
```

- **手繪**：自由塗畫
- **直線**：拖曳時即時預覽，放開定案
- **螢光**：半透明加粗，蓋在文字上仍讀得到字
- **⊕ 自訂**：開啟色票，任何顏色都能選
- **復原**：最多 8 步

筆跡存在該題，**下次做到這題（公布答案後）會自動顯示**。要捲動長題目時，按「✓ 完成」關閉畫筆即可。

### 申論題

作答框可直接打字。按「顯示參考答案」後，以**國考答題紙樣式**呈現：稿紙橫線、段落自動編號。下方顯示**關鍵概念檢測**（命中數 + 綠/紅標籤）。

> 參考答案用**換行**分段。在題庫填答案時用「一、」「(一)」開頭再換行，就會呈現標準申論架構。

---

## 四、新增題目

### 大量新增（推薦）

1. 填科目、年度、考試別
2. 貼上題目文字
3. 可選填答案列：`1.D 2.A 3.C`（**要帶題號**，跳號沒關係；不要用純字母 `DACB`）
4. 可選填編／章／節，批次套用整批
5. 按「解析預覽」確認再儲存

**貼題格式：**
```
1. 題幹敘述？
(A)選項一
(B)選項二
(C)選項三
(D)選項四
```

規則：
- 題號 = 行首「數字＋`.`／`、`／空格」
- 選項 = (A)~(H)，同行分行皆可
- 題幹中的 `？：（）《》①②③` 一律保留不截斷
- 圈數字 ①②③ 一律當題目內編號，不會被當選項
- PDF 特殊字型的選項符號會自動辨識

### 逐一新增

填題幹、選項、答案、科目／年度／考試別、**關鍵概念**（高頻錯誤分析靠這個，建議都填）、備註、相關法條。

**題組**（共用大題幹）：勾選後每個小題共用同一段大題幹，靠題組編號綁定。

---

## 五、法規資料庫

### 四種類別

法規條文｜SOP｜補充資料｜函釋（後三者可直接上傳圖片）

### 大量新增

填法律名稱、類別、來源，貼上條文：

```
第一編 總則
第一章 法例
第1條 （立法目的）
本法所稱……
第7條之1
```

- 編／章／節會**自動繼承**到後面條文
- 支援中文數字、阿拉伯數字、容忍空格
- **一定要有「第X條」**，否則解析不出來
- 同一部法規重複匯入會問「覆蓋」，安全

### 法條連結

題目的「關聯法條」點下去會**跳出彈窗**（不離開當前畫面）：

| 寫法 | 行為 |
|------|------|
| `警察法第2條` | 直接顯示該條內容 |
| `警察法§2` | 同上（§ 是簡寫，支援 `§2-1`） |
| `警察法` | 列出該法規**所有條文清單**，點任一條展開 |

### 搜尋

法規搜尋框支援 `法規名§條號` 精準定位，例如 `警察法§2`、`警察法§2-1`。

---

## 六、朗讀功能

**書庫**：支援系統語音與 Azure 語音。從當前位置開始，唸完自動翻頁，讀到書末停止。播放中可調語速、切換聲音。

**成長區**：英語教材逐句朗讀，播放中可調語速、點任一句跳讀。

**Azure 用量估算**：設定頁顯示本月字元數與免費額度百分比（達 80% 轉警示色，跨月自動歸零）。

> 這是**估算值**，實際用量以 Azure 後台為準。免費層每月 50 萬字元，朗讀整本電子書約佔 30%。

---

## 七、返回鍵行為

按返回會**逐層關閉**，不會直接退出：

```
確認框 → FAB → 加號選單 → 電子書閱讀器 → 播放器
→ 彈窗 → 法條檢視 → 英語閱讀器 → 標註模式
→ 答題畫面 → 上一頁 → 首頁 → 離開 App
```

例如：資料庫 → 看法條 → 返回（回資料庫）→ 返回（回首頁）→ 返回（離開）。

---

## 八、備份

| 方式 | 涵蓋 | 建議頻率 |
|------|------|---------|
| **雲端備份** | 9 項純文字資料 | **每週** |
| **本機備份** | 13 項全部（含實體檔案） | **每月／換機前** |

**雲端包含**：題庫（含手寫標註、複習進度）、法規、答題紀錄、設定、倒數日、使用統計、英語教材、單字本、健康數據。
**雲端不含**：電子書、影音、參考書、教材影音 —— 大型檔案走雲端會失敗。

**本機備份的資料夾結構：**
```
你選的資料夾/
├── exam_data.json    ← 所有純文字資料（題庫、法條、英語教材…）
├── ebooks/           ← 休閒電子書
├── media/            ← 休閒影音
├── refbooks/         ← 學習參考書
└── learnmedia/       ← 學習教材影音
```

> **英語教材是文字，存在 `exam_data.json` 裡**，沒有獨立資料夾。

---

## 九、題幹標記語法

| 標記 | 效果 |
|------|------|
| `___` | 填空底線 |
| `[[提示]]` | 填空（含提示） |
| `**文字**` | 畫線強調 |
| `//文字//` | 粗體 |
| `>> 文字` | 段落縮排 |

點新增題目標題旁的 ⓘ 可隨時查看。

---

## 十、學習規劃（首頁）

- **里程碑**：分階段複習計畫，可設定各階段起訖日
- **每日任務**：可綁定自動完成條件，按 🔄 依實際資料自動勾選
- **成長軌跡熱力圖**：過去 35 天學習時間＋任務完成度

---

## 十一、設定裡的維護工具

**重建條號索引** —— **匯入大量法條後請按一次**。它會：
1. 重算法條排序值（第10條之2 → 10002）
2. 修正編章節的孤立錯標（多數決）
3. 重建法規與題目的搜尋索引

**主題** —— 標準（深色）／電子紙（已針對 e-ink 優化：停用動畫、高對比、大字體）

---

## 十二、常見狀況

| 狀況 | 解法 |
|------|------|
| 更新後畫面沒變 | App 有兩層快取，**完全關閉再開** |
| 法條連結查無資料 | 設定 → **重建條號索引** |
| 標註畫不出來 | 確認已按 ✏️ 開啟畫筆（開啟時按鈕變色、下方出現工具列） |
| 手寫標註看不到 | 筆跡只在**公布答案後**才顯示，避免提前洩題 |
| 高頻錯誤分析沒東西 | 題目沒填 `keywords` |

---

## 十三、資料安全

- 所有資料存本機（IndexedDB），不上傳伺服器
- **別清網站資料**（會清掉題庫），有備份就不怕
- 匯出的 JSON 可交給程式批次處理（規格見 `exam_data_spec.md`）

---
---

# 第二部分：開發維護

## 檔案功能對照表

### JS（19 個）

| 檔案 | 負責功能 | 常見修改情境 |
|------|---------|-------------|
| **db.js** | IndexedDB schema、`APP_VERSION`、選題池、遺忘曲線計算 | 加資料表、改欄位、**每次更新都要改版本號** |
| **utils.js** | 大量匯入解析器、題幹標記渲染、`art2n` 條號換算、`getWrong` 危險題判定 | 貼題匯入錯亂、標記語法、法條排序 |
| **data.js** | 首頁、熱力圖、題庫導覽、新增題目／法規、法規資料庫、**法條連結彈窗** | 新增表單、列表顯示、搜尋、法條連結 |
| **quiz.js** | 答題流程、科目多選、**手寫標註**、文字備註、申論答題紙 | 答題流程、複習排程、標註／備註 |
| **app.js** | 頁面切換、FAB、加號選單、**返回鍵處理（11 層）** | 導覽邏輯、返回鍵行為 |
| **books.js** | 書庫、epub 閱讀器、字級調整、eink 閱讀主題 | 電子書顯示、翻頁、字級 |
| **tts.js** | 語音朗讀（系統／Azure）、電子書自動翻頁續讀、**Azure 用量估算** | **朗讀重複／間隔／不翻頁**都在這 |
| **english.js** | 英語學習庫、**逐句朗讀（世代序號防跳句）**、發音評估 | 英語功能、英語朗讀 |
| **media.js** | 影音庫、播放器 | 影音播放 |
| **settings.js** | 設定頁、**雲端／本機備份與還原** | 備份還原、設定選項 |
| **stats.js** | 統計分析、高頻關鍵概念、AI 分析匯出 | 統計圖表、錯誤分析 |
| **dtask.js** | 每日任務、自動完成判斷 | 每日任務 |
| **plan.js** | 計畫表（行事曆時間軸） | 計畫表 |
| **milestone.js** | 複習里程碑、學習規劃分頁 | 里程碑 |
| **countdown.js** | 倒數計時、每日格言 | 倒數 |
| **fitness.js** | 運動健康紀錄 | 運動／健康 |
| **layout.js** | 響應式版面 | 版面尺寸 |
| **epub.min.js** / **jszip.min.js** | 第三方函式庫 | **不要手動改** |

### CSS（8 個）

| 檔案 | 負責介面 |
|------|---------|
| **base.css** | 全站共用：首頁、熱力圖、每日任務、按鈕／字級／顏色變數 |
| **quiz.css** | 答題畫面、**畫筆工具列**、**申論答題紙**、科目多選、標記語法 |
| **books.css** | 書庫、epub 閱讀器 |
| **eink.css** | **電子紙專屬**：全域停用動畫、高對比、大字體 |
| **media.css** | 影音庫、播放器、toast 提示 |
| **english.css** | 英語學習庫 |
| **fitness.css** | 運動健康 |
| **splash.css** | 啟動畫面 |

---

## 快速查找：症狀 → 檔案

| 症狀 | 先看這個檔 |
|------|-----------|
| 大量貼題匯入錯亂 | `utils.js`（parseQuestions） |
| 逐一新增題目存不進去 | `data.js`（saveQ） |
| 法規匯入或排序錯 | `utils.js`（art2n）＋ `data.js`（saveLaw / parseLawText） |
| **法條連結點了沒反應／查無資料** | `data.js`（showLawPop）＋ 設定按「重建條號索引」 |
| 答題選項／複習排程異常 | `quiz.js` |
| **手寫標註畫不出來／位移** | `quiz.js`（_inkBind）＋ `quiz.css`（.qink） |
| 電子書朗讀重複／卡頓／不翻頁 | `tts.js` |
| **英語朗讀跳句／調語速異常** | `english.js`（_speakSentence 世代序號） |
| 電子書翻頁／字級異常 | `books.js` |
| 電子紙殘影／耗電 | `eink.css` |
| **返回鍵行為異常** | `app.js`（_handleBackLayer） |
| **備份缺資料／還原失敗** | `settings.js`（備份與還原必須對稱） |
| 首頁熱力圖／每日任務異常 | `data.js` ＋ `base.css` ＋ `dtask.js` |
| 版本更新後功能沒生效 | `db.js`／`sw.js`（先確認版本號有無同步） |
| 高頻錯誤分析沒東西 | `stats.js`，但根源通常是題目沒填 keywords |

---

## 改動時的固定規則

1. **版本號同步** —— `js/db.js` 與 `sw.js` 的 `APP_VERSION` 必須**同時改且一致**，否則快取不會更新。

2. **白名單** —— 各模組是 IIFE，新增給 HTML `onclick` 用的函式**必須加進檔尾的匯出物件**，否則 `not defined`：
   - `quiz.js` → `const Quiz = {...}`
   - `data.js` → `const DataMod = {...}`
   - `app.js` → `const App = {...}`
   - `english.js` → `const English = {...}`

3. **匯出必須排在初始化之前** —— 若把 `Object.assign(window, ...)` 放在立即執行的初始化之後，初始化一旦拋錯就會中斷 IIFE，**整個模組的函式全部變成 not defined**（連鎖失效）。初始化也要包 `try/catch`。

4. **searchBlob 要重建** —— 改了來源欄位就要重組，公式見 `exam_data_spec.md`。題目版與法規版公式不同。

5. **條號比對用 `art2n`** —— `articleNumber` 是「主號×1000＋子號」格式，不可直接與純數字比對。缺漏時要 fallback：`(l.articleNumber || art2n(l.article))`。

6. **備份與還原必須對稱** —— 新增資料表時，`gdriveBackup`／`gdriveRestore`／`localBackup`／`localRestore` **四處都要同步加**，否則還原後資料消失。

7. **一次改動常跨多檔** —— 部署時**全部一起上傳**，只傳部分會出現 HTML 有新按鈕但 JS 沒對應函式的情況。

---

## 相關文件

| 文件 | 用途 |
|------|------|
| `README.md` | 本檔：操作說明 + 開發維護 |
| `exam_data_spec.md` | 備份檔結構與欄位規格（給程式批次處理用） |

---

## 倒數

- 升官等：**2026-11-07**
- 佐二類：**2027-03-06**

工具已經完備，剩下的是每天打開它刷題。

---

*版本 4.6.0*
